# -2-Python-Mathematical-Logical-and-Membership-Operators 
## Арифметические операции в Python для числовых типов данных

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=%23+arithmetic+;print(6+*+(2+%2B+2));c+%3D+2+**+4)](https://git.io/typing-svg)

В Python для числовых типов данных (таких как целые числа int, числа с плавающей запятой float и комплексные числа complex) доступны различные математические операции. Вот основные из них: <br> <br>
Таблица с математическими операторами: <br>
| Оператор | Описание | Пример использования |
|----------|----------|----------------------|
| `+`      | Сложение. Суммирует значения слева и справа от оператора. | 1) `5 + 5`, вывод: `10`<br>2) `a + b`, вывод зависит от значений переменных `a` и `b`. |
| `-`      | Вычитание. Вычитает значение справа от оператора из значения слева от оператора. | 1) `10 - 3`, вывод: `7`<br>2) `a - b`, вывод зависит от значений переменных `a` и `b`. |
| `*`      | Умножение. Умножает значения слева и справа от оператора. | 1) `4 * 3`, вывод: `12`<br>2) `a * b`, вывод зависит от значений переменных `a` и `b`. |
| `/`      | Деление. Делит значение слева от оператора на значение справа от оператора, результат – число с плавающей точкой. | 1) `10 / 2`, вывод: `5.0`<br>2) `a / b`, вывод зависит от значений переменных `a` и `b`. |
| `//`     | Целочисленное деление. Делит значение слева от оператора на значение справа от оператора, результат – целая часть от деления. | 1) `10 // 3`, вывод: `3`<br>2) `a // b`, вывод зависит от значений переменных `a` и `b`. |
| `%`      | Остаток от деления. Возвращает остаток от деления значения слева от оператора на значение справа от оператора. | 1) `10 % 3`, вывод: `1`<br>2) `a % b`, вывод зависит от значений переменных `a` и `b`. |
| `**`     | Возведение в степень. Возводит значение слева от оператора в степень, указанную справа от оператора. | 1) `2 ** 3`, вывод: `8`<br>2) `a ** b`, вывод зависит от значений переменных `a` и `b`. |


**Сложение** (+): Складывает два числа. <br>
`print(5 + 3)  # 8` <br><br>
**Вычитание** (-): Вычитает одно число из другого. <br>
`print(5 - 3)  # 2` <br><br>
**Умножение** (*): Умножает два числа. <br>
`print(5 * 3  # 15)` <br><br>
**Деление** (/): Делит одно число на другое, результатом всегда будет float. <br>
`print(5 / 3 ) # 1.6666666666666667` <br><br>
**Целочисленное деление** (//): Делит два числа, возвращает только целую часть. <br>
`print(5 // 3)  # 1` <br><br>
**Остаток от деления** (%): Возвращает остаток от деления. <br>
`print(5 % 3)  # 2` <br><br>
**Возведение в степень** (**): Возводит число в степень. <br>
`print(5 ** 3)  # 125` <br>

Так можно делать и со значением в переменных, например: <br>
```Python
a = 7
b = 4
print(a - b)  # 3
```
Ещё пример:
```Python
number_1 = 13
number_2 = 2
print(number_1 / number_2)  # 6.5
print(number_1 // number_2)  # 6 
print(number_1 % number_2)  # 1
```

## Арифметические операции в Python для нечисловых типов данных

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=my_str+%3D+%22Hello%22;my_str+%2B%3D+%22!%22;print(%5B0%2C+1%5D+%2B+%5B2%2C+3%5D))](https://git.io/typing-svg)

Арифметические операции в Python применимы не только к числовым типам данных, но и к некоторым нечисловым типам, таким как строки (str), списки (list), кортежи (tuple) и множества (set). Вот как они работают:

**1. Конкатенация строк (str)** <br>
Сложение (+):

Оператор `+` используется для объединения (конкатенации) строк.
```Python
s1 = "Hello"
s2 = "World"
result = s1 + " " + s2  # result = "Hello World"
```
Умножение (*):

Оператор `*` используется для повторения строки заданное количество раз.
```Python
s = "Ha"
result = s * 3  # result = "HaHaHa"
```
**2. Операции со списками (list)** <br>
Сложение (+):

Оператор `+` используется для объединения двух списков в один.
```Python
list1 = [1, 2, 3]
list2 = [4, 5, 6]
result = list1 + list2  # result = [1, 2, 3, 4, 5, 6]
```
Умножение (*):

Оператор `*` используется для повторения элементов списка.
```Python
list1 = [1, 2, 3]
result = list1 * 2  # result = [1, 2, 3, 1, 2, 3]
```
**3. Операции с кортежами (tuple)** <br>
Сложение (+):

Оператор `+` используется для объединения двух кортежей в один.
```Python
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
result = tuple1 + tuple2  # result = (1, 2, 3, 4, 5, 6)
```
Умножение (*):

Оператор `*` используется для повторения элементов кортежа.
```Python
tuple1 = (1, 2, 3)
result = tuple1 * 2  # result = (1, 2, 3, 1, 2, 3)
```
**4. Операции с множествами (set)** <br>
Объединение (|):

Оператор `|` используется для объединения двух множеств, возвращая новое множество с уникальными элементами.
```Python
set1 = {1, 2, 3}
set2 = {3, 4, 5}
result = set1 | set2  # result = {1, 2, 3, 4, 5}
```
Пересечение (&):

Оператор `&` используется для нахождения пересечения двух множеств.
```Python
set1 = {1, 2, 3}
set2 = {3, 4, 5}
result = set1 & set2  # result = {3}
```
Разность (-):

Оператор `-` используется для нахождения разности между двумя множествами.
```Python
set1 = {1, 2, 3}
set2 = {3, 4, 5}
result = set1 - set2  # result = {1, 2}
```
Симметрическая разность (^):

Оператор `^` используется для нахождения элементов, которые присутствуют в одном из множеств, но не в обоих.
```Python
set1 = {1, 2, 3}
set2 = {3, 4, 5}
result = set1 ^ set2  # result = {1, 2, 4, 5}
```

## Оператор присвоения

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=variable+%3D+value;x+%3D+8;y+%3D+12;luck+%3D+%22%F0%9F%8D%80%22)](https://git.io/typing-svg)

Оператор, который уже вам знаком из предыдущего репозитория.
>Присваивание: Когда вы присваиваете значение переменной (x = 10), Python создаёт объект со значением 10 и связывает его с именем x.

Наиболее полное определение:
Оператор присвоения в Python используется для присвоения значения переменной. Основной оператор присвоения – это знак равенства =. Кроме того, существуют составные операторы присвоения, которые комбинируют операцию присвоения с арифметическими операциями.

**Основной оператор присвоения** (`=`) <br>
Присваивает значение, указанное справа, переменной слева. <br>
Пример: <br>
```Python
x = 10  # Переменной x присваивается значение 10
```
<br>

Но совершенно необязательно присваивать только одно значение, в ходе выполнения программы чиисла могут меняться.

## Составные операторы присвоения

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=a+%3D+7;b+%3D+3;a+%2B%3D+b)](https://git.io/typing-svg)

Составные операторы присвоения упрощают запись и выполнение арифметических операций с переменными. Они включают в себя арифметическую операцию и оператор присвоения.

`+=`: Прибавляет значение справа к переменной и присваивает результат этой же переменной.
```Python
x = 5
x += 3  # Эквивалентно x = x + 3, теперь x равно 8
```
`-=:` Вычитает значение справа из переменной и присваивает результат этой же переменной.
```Python
x = 5
x -= 2  # Эквивалентно x = x - 2, теперь x равно 3
```
`*=`: Умножает переменную на значение справа и присваивает результат этой же переменной.
```Python
x = 5
x *= 3  # Эквивалентно x = x * 3, теперь x равно 15
```
`/=`: Делит переменную на значение справа и присваивает результат этой же переменной.
```Python
x = 10
x /= 2  # Эквивалентно x = x / 2, теперь x равно 5.0
```
`//=`: Выполняет целочисленное деление переменной на значение справа и присваивает результат этой же переменной.
```Python
x = 10
x //= 3  # Эквивалентно x = x // 3, теперь x равно 3
```
`%=`: Делит переменную на значение справа и присваивает остаток от деления этой же переменной.
```Python
x = 10
x %= 3  # Эквивалентно x = x % 3, теперь x равно 1
```
`**=`: Возводит переменную в степень значения справа и присваивает результат этой же переменной.
```Python
x = 2
x **= 3  # Эквивалентно x = x ** 3, теперь x равно 8
```

| Краткая форма       | Полная форма           |
|---------------------|------------------------|
| `x += 1`            | `x = x + 1`            |
| `x -= 1`            | `x = x - 1`            |
| `x *= 2`            | `x = x * 2`            |
| `x /= 2`            | `x = x / 2`            |
| `x //= 2`           | `x = x // 2`           |
| `x %= 3`            | `x = x % 3`            |
| `x **= 3`           | `x = x ** 3`           |

Ещё примеры:
```Python
number_1 = 7
number_2 = 4
number_1 += number_2
print(number_1) 
```
👆 здесь, начиная с 3 строки кода, переменная number_1 имеет значение 11.
Стоит обратить внимание, что и тип данных тоже может поменяться, к примеру:
```Python
num_1 = 8
num_2 = 2.6
num_1 += num_2
print(num_1)
```
Теперь, `num_1` - это число с плавающей точкой (float).
Исходя из этого, немного дополним прошлую тему и узнаем, как ещё можно поменять тип данных.

## Изменение типов данных

<a href="https://git.io/typing-svg"><img src="https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=int_num+%3D+7;float_num+%3D+float(int_num));str('_random_text_');int(999)" alt="Typing SVG" /></a>

В Python можно явно и неявно изменять тип данных переменной, что называется "приведением типов". Давайте рассмотрим, как это работает для различных типов данных.

**1. Из int в float**
Неявное приведение (при выполнении операций):
Когда вы выполняете арифметическую операцию между int и float, результат автоматически будет типа float.
```Python
x = 5   # int
y = 2.5 # float
z = x + y  # z станет float, z = 7.5
```
Явное приведение:
Для явного приведения целого числа в число с плавающей точкой используется функция float().
```Python
x = 5   # int
y = float(x)  # y станет float, y = 5.0
```
**2. Из float в int**
Неявное приведение:
В Python не происходит автоматического приведения float в int при выполнении операций. Если нужно преобразовать float в int, это нужно сделать явно.
Явное приведение:
Для явного приведения числа с плавающей точкой в целое используется функция int(). Это отбрасывает дробную часть.
```Python
x = 7.9  # float
y = int(x)  # y станет int, y = 7
```
**3. Из str в int или float**
Явное приведение:
Строка, содержащая числовое значение, может быть приведена к int или float с использованием функций int() и float().
При этом строка должна состоять только из цифр (для int) или содержать допустимый формат числа (для float), иначе произойдет ошибка.
```Python
s = "42"
x = int(s)  # x станет int, x = 42
```
```Python
s = "3.14"
y = float(s)  # y станет float, y = 3.14
```
Ошибка:
```Python
s = "abc"
x = int(s)  # Вызовет ValueError: invalid literal for int()
```
**4. Из int или float в str**
Явное приведение:
Для преобразования числового значения в строку используется функция str().
```Python
x = 42   # int
s = str(x)  # s станет str, s = "42"
```
```Python
y = 3.14  # float
s = str(y)  # s станет str, s = "3.14"
```
**5. Из bool в int или str**
Явное приведение:
Логическое значение True можно привести к 1, а False к 0.
```Python
b = True
x = int(b)  # x станет int, x = 1
```
```Python
b = False
s = str(b)  # s станет str, s = "False"
```
**6. Из int в bool**
Неявное приведение:
В логическом контексте (например, в условных операторах) числа автоматически приводятся к типу `bool`: 0 становится `False`, а любое ненулевое число — `True`.
```Python
x = 0
print(bool(x))  # Вывод: False
```
```Python
x = 42
print(bool(x))  # Вывод: True
```
Явное приведение:
Можно явно привести int к bool.
```Python
x = 0
b = bool(x)  # b станет False
```
```Python
y = 42
b = bool(y)  # b станет True
```

## Операторы сравнения

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=print(7+%3E+10000)++%23+False;a+!+%3D+b)](https://git.io/typing-svg)

| Оператор | Описание | Пример использования |
|----------|----------|----------------------|
| `==`     | Равно. Проверяет, равны ли значения слева и справа от оператора. | 1) `5 == 5`, вывод: `True`<br>2) `a == b`, вывод зависит от значений переменных `a` и `b`. |
| `!=`     | Не равно. Проверяет, не равны ли значения слева и справа от оператора. | 1) `5 != 3`, вывод: `True`<br>2) `a != b`, вывод зависит от значений переменных `a` и `b`. |
| `>`      | Больше. Проверяет, больше ли значение слева от оператора, чем справа. | 1) `7 > 3`, вывод: `True`<br>2) `a > b`, вывод зависит от значений переменных `a` и `b`. |
| `<`      | Меньше. Проверяет, меньше ли значение слева от оператора, чем справа. | 1) `3 < 7`, вывод: `True`<br>2) `a < b`, вывод зависит от значений переменных `a` и `b`. |
| `>=`     | Больше или равно. Проверяет, больше ли или равно значение слева от оператора значению справа. | 1) `5 >= 5`, вывод: `True`<br>2) `a >= b`, вывод зависит от значений переменных `a` и `b`. |
| `<=`     | Меньше или равно. Проверяет, меньше ли или равно значение слева от оператора значению справа. | 1) `3 <= 4`, вывод: `True`<br>2) `a <= b`, вывод зависит от значений переменных `a` и `b`. |

Операторы сравнения в Python используются для сравнения значений. Они возвращают логические значения `True` или `False` в зависимости от результатов сравнения.

**Операторы сравнения** <br>

Равно (`==`): Проверяет, равны ли два значения.
```Python
print(5 == 3) # False
print(4 == 4) # True
```
Не равно (`!=`): Проверяет, не равны ли два значения.
```Python
print(5 != 3) # True
print(4 != 4) # False
```
Больше (`>`): Проверяет, больше ли одно значение, чем другое.
```Python
print(5 > 3) # True
print(2 > 3) # False
```
Меньше (`<`): Проверяет, меньше ли одно значение, чем другое.
```Python
print(5 < 3) # False
print(2 < 3) # True
```
Больше или равно (`>=`): Проверяет, больше ли или равно одно значение другому.
```Python
print(5 >= 3) # True
print(3 >= 3) # True
print(2 >= 3) # False
```
Меньше или равно (`<=`): Проверяет, меньше ли или равно одно значение другому.
```Python
print(5 <= 3) # False
print(3 <= 3) # True
print(2 <= 3) # True
```
Эти операторы в будущем будут используются в условных выражениях, циклах и многих других конструкциях Python, позволяя проверять условия и принимать решения в коде.

Таким же образом можно сравнить строки, списки и т.д. К примеру:
```Python
print('abc' != 'aaa')  # True
print([1, 2] == [1, 2])  # True
print((0, 0) > (1, 2))  # False
```
**Важное замечание**

Мы не можем сравнить строки на больше\меньше.
Хотя операторы сравнения могут использоваться с различными типами данных, включая строки, списки и кортежи, для сравнения длины таких структур данных используется функция `len()`

|<img src="https://i.gifer.com/origin/06/0691766358720472929257b565c761f0_w200.gif">|`len()` возвращает количество элементов в объекте (массива, кортежа, словаря) или количество символов в строке. Рассмотрим пример, где сравнивается количество символов/элементов.
|------------|----------------------------------|
||`print(len('abc'))  # 3`<br>`print(len([1, 2, 3]) > len([1, 2]))  # True`<br>`print(len('hello') == len('world'))  # True`<br>`print('stroka' < 'ssttrrookkaa')  # False`|

## Операторы членства

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines='a'+not+in+'Python'++%23+True;0+in+%5B1%2C+2%2C+3%5D++%23+False;'%F0%9F%8D%AD'+in+('%F0%9F%8D%95'%2C+'%F0%9F%8D%94'%2C+'%F0%9F%8D%AD')++%23+True)](https://git.io/typing-svg)

| Оператор  | Описание                                | Примеры                  | Вывод |
|-----------|-----------------------------------------|--------------------------|-------|
| `in`      | Проверяет, содержится ли элемент в коллекции | `'a' in 'apple'`        | `True`  |
|           |                                         | `3 in [1, 2, 3]`        | `True`  |
|           |                                         | `5 in {1, 2, 3}`        | `False` |
| `not in`  | Проверяет, не содержится ли элемент в коллекции | `'z' not in 'apple'`    | `True`  |
|           |                                         | `4 not in [1, 2, 3]`    | `True`  |
|           |                                         | `2 not in {1, 2, 3}`    | `False` |


Операторы членства в Python используются для проверки наличия элемента в коллекциях данных, таких как списки, строки, множества и кортежи. Эти операторы помогают узнать, содержится ли конкретный элемент в коллекции или нет, что полезно при выполнении различных проверок и условий в коде.

`in`
Оператор `in` проверяет, содержится ли элемент в коллекции. Если элемент присутствует, результатом будет `True`; если нет — `False`.

Примеры использования:
```Python
# Проверка наличия элемента в строке
print('a' in 'apple')   # True, так как 'a' содержится в 'apple'

# Проверка наличия элемента в списке
print(3 in [1, 2, 3])   # True, так как 3 содержится в списке [1, 2, 3]

# Проверка наличия элемента в множестве
print(5 in {1, 2, 3})   # False, так как 5 не содержится в множестве {1, 2, 3}

# Проверка наличия элемента в кортеже
print('x' in ('a', 'b', 'c'))  # False, так как 'x' не содержится в кортеже ('a', 'b', 'c')
```

`not in`
Оператор `not in` проверяет, не содержится ли элемент в коллекции. Если элемент отсутствует, результатом будет `True`; если присутствует — `False`.

Примеры использования:
```Python
# Проверка отсутствия элемента в строке
print('z' not in 'apple')   # True, так как 'z' не содержится в 'apple'

# Проверка отсутствия элемента в списке
print(4 not in [1, 2, 3])   # True, так как 4 не содержится в списке [1, 2, 3]

# Проверка отсутствия элемента в множестве
print(2 not in {1, 2, 3})   # False, так как 2 содержится в множестве {1, 2, 3}

# Проверка отсутствия элемента в кортеже
print('y' not in ('a', 'b', 'c'))  # True, так как 'y' не содержится в кортеже ('a', 'b', 'c')
```
## Итог

Это не все операторы Python, но самые основные. Есть ещё побитовые операторы, которые работают с отдельными битами чисел и выполняют побитовые операции. Но в данном предмете они рассмотрены не будут. Перед выполнением заданий обязательно ознакомьтесь с кодом в файлах.

## Задания для самостоятельного выполнения.

[![Typing SVG](https://readme-typing-svg.herokuapp.com?font=Fira+Code&pause=1000&color=1B9C0B&width=435&lines=%23+from+theory+to+practice;variable_1+%3D+True;variable_2+%3D+False;num_1%2C+num_2+%3D+71%2C+36.7;num_1+**%3D+2)](https://git.io/typing-svg)

**Задание 1**

Создайте две переменные `a` и `b`, присвойте им значения 5 и 10 соответственно. Затем переприсвойте переменной `a` значение переменной `b`. После этого переприсвойте переменной `b` значение 20. Выведите на экран значения переменных `a` и `b`.

**Задание 2**

Создайте переменные `x`, `y` и `z`. Присвойте переменной `x` значение 7, переменной `y` значение 3, а `z` значение 0. Используя арифметические операции, выполните следующие действия:

1. Найдите сумму `x` и `y` и выведите результат.
2. Найдите разность `x` и `y` и выведите результат.
3. Умножьте `x` на `y` и выведите результат.
4. Поделите `x` на `y` с остатком и выведите результат.
5. Поделите `x` на `y` без остатка и выведите результат.
6. Возведите `x` в степень `y` и выведите результат.
7. Задайте переменной `z` значение `x**2 - (x * y)` и выведете значение `z` на экран. 

**Задание 3**

Создайте переменные `m` и `n` и присвойте им значения 8 и 4 соответственно. Затем выполните следующие действия:

1. Присвойте переменной `m` сумму переменных `m` и `n`.
2. Присвойте переменной `n` произведение переменной `m` и числа 2.
3. Присвойте переменной `m` остаток от деления переменной `n` на переменную `m`.
4. Переприсвойте переменной `n` разность переменной `n` и переменной `m`.
5. Выведите на экран итоговые значения переменных `m` и `n`.

**Задание 4**

Создайте переменные `a`, `b` и `c`. Присвойте переменной `a` значение 12, переменной `b` значение 8, а переменной `c` значение 12. Используя операции сравнения, выполните следующие действия:

1. Проверьте, больше ли `a`, чем `b`, и выведите результат.
2. Проверьте, меньше ли `b`, чем `a`, и выведите результат.
3. Проверьте, равно ли `a` значению переменной `c`, и выведите результат.
4. Проверьте, не равно ли `b` значению переменной `c`, и выведите результат.
5. Проверьте, больше ли или равно `a` значению переменной `c`, и выведите результат.
6. Проверьте, меньше ли или равно `b` значению переменной `a`, и выведите результат.

**Задание 5**

Дан список, обозначающий рюкзак.

backpack = ['ручка', 'тетрадь', 'учебник', 'карандаш', 'фломастеры', 'линейка', 'ластик']

С помощью операторов членства проверьте:
1. Есть ли в рюкзаке карандаш.
2. Нет ли в рюкзаке яблока.
3. Выведете длину рюкзака (количество элементов списка).

